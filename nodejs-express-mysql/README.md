
---

# Backend – Run Guide

## 📦 Prerequisites

* Node.js (v16+ recommended)

Check version:

```bash
node -v
```

---

## 🚀 Install Dependencies

From the backend folder (`nodejs-express`):

```bash
npm install
```

---

## ▶️ Start Server

If `server.js` is the entry file:

```bash
node server.js
```

Or (if defined in package.json):

```bash
npm start
```

---

## 🌐 Access API

Server will run on something like:

```
http://localhost:3000
```

(Check `server.js` for the exact port.)
